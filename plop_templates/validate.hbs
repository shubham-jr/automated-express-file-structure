const Joi = require('joi');
// const { objectId } = require('./custom.validation');

const create{{camel name}} = {
  body: Joi.object({
   
  })
};

const update{{camel name}} = {
  body: Joi.object({
   
  })
};

module.exports = { create{{camel name}} , update{{camel name}} };
