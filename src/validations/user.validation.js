const Joi = require('joi');
// const { objectId } = require('./custom.validation');

const createUser = {
  body: Joi.object({
   
  })
};

const updateUser = {
  body: Joi.object({
   
  })
};

module.exports = { createUser , updateUser };
